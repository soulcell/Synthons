<Page
    x:Class="Synthons.WinUI3.Views.EmployeesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Command="{x:Bind ViewModel.LoadEmployeesCommand}">Read</Button>
        <Button Grid.Column="1">Create</Button>
        <Button Grid.Column="2">Update</Button>
        <Button Grid.Column="3">Delete</Button>
        <controls:DataGrid 
            Grid.Row="1"
            Grid.ColumnSpan="4"
            Margin="0 10"
            AutoGenerateColumns="True"
            ItemsSource="{x:Bind ViewModel.Employees}"/>
    </Grid>
</Page>
